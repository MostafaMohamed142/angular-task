export interface ImageUploadStateModel {
    selectedImage: string | null;
  }
  export interface Time {
    value: string;
    viewValue: string;
  }
  export default interface counter{
    count : number,
  }